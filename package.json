{
  "name": "meshy",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "check-types": "tsc --noEmit",
    "build": "tsc",
    "build:web": "cd frontend && vite build",
    "build:all": "pnpm run build && pnpm run build:web",
    "dev:web": "cd frontend && vite",
    "dev:server": "USE_MOCK=true tsx watch index.ts",
    "start": "pnpm run build && pnpm run build:web && node --env-file=.env dist/index.js",
    "start:dev": "pnpm run build && concurrently -i -n \"server,client\" -c \"magenta,cyan\" \"USE_MOCK=true node --env-file=.env dist/index.js\" \"pnpm run dev:web\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "oxlint --deny correctness --deny perf --allow no-await-in-loop",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "prepare": "husky"
  },
  "dependencies": {
    "@meshtastic/core": "^2.6.7",
    "@meshtastic/transport-node": "^0.0.2",
    "@picocss/pico": "^2.0.6",
    "express": "^5.2.1",
    "lodash": "^4.18.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^20.19.40",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "concurrently": "^9.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.2",
    "oxfmt": "^0.48.0",
    "oxlint": "^1.63.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "vite": "^5.4.10"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "oxlint --deny correctness --deny perf --allow no-await-in-loop",
      "oxfmt"
    ]
  }
}
